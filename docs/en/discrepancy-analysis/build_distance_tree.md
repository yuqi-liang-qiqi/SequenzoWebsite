# `build_distance_tree()`

This page has been moved from:

- `/en/group-comparison/build_distance_tree`

The method documentation is unchanged and still covers discrepancy-based distance tree building.
