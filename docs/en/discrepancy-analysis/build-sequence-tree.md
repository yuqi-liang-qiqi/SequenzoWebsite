# `build_sequence_tree()`

This page has been moved from:

- `/en/group-comparison/build_sequence_tree`

The method documentation is unchanged and still covers sequence-based tree construction for discrepancy analysis.
