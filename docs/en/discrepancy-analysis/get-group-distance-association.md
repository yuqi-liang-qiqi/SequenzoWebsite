# `get_group_distance_association()`

This page has been moved from:

- `/en/group-comparison/get_group_distance_association`

The function content is unchanged and still documents the discrepancy-based association test between sequence distances and group labels.
